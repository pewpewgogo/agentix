# Generated token-budget results

`npm run sandbox:token-budget` writes `token-budget-latest.json` and
`token-budget-latest.md` here. They are ignored because local calibration output
is not benchmark evidence. CI uploads the same files as workflow artifacts.
