{
  "name": "@agentixdev/sandbox-agentix-notes",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false && tsc -p tsconfig.test.json --pretty false",
    "test": "vitest run --root ../.. sandbox/notes-app"
  },
  "dependencies": {
    "@agentixdev/core": "0.0.0"
  },
  "devDependencies": {
    "@agentixdev/testing": "0.0.0",
    "vitest": "4.1.10"
  }
}
