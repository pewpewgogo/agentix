{
  "name": "@agentix/compiler",
  "version": "0.1.0",
  "description": "Static architecture analysis and bounded context projection for Agentix applications.",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pewpewgogo/agentix.git",
    "directory": "packages/compiler"
  },
  "homepage": "https://pewpewgogo.github.io/agentix/",
  "bugs": {
    "url": "https://github.com/pewpewgogo/agentix/issues"
  },
  "engines": {
    "node": ">=22.12.0 <25"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "!dist/* *",
    "!dist/**/* *",
    "!dist/*.tsbuildinfo*",
    "!dist/**/*.tsbuildinfo*",
    "!dist/*.test.*",
    "!dist/**/*.test.*",
    "README.md",
    "API.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false && tsc -p tsconfig.test.json --pretty false",
    "test": "vitest run --root ../.. packages/compiler"
  },
  "//": "typescript is pinned to an exact version on purpose: the compiler consumes the unstable typescript/unstable/sync native-compiler API, which carries no stability guarantees between releases. src/ts.ts guards the import at load time; revalidate that guard before moving this pin.",
  "dependencies": {
    "typescript": "7.0.2"
  }
}
